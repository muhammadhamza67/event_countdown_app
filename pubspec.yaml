name: event_countdown_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  flutter_sound_lite: ^8.5.0
  path_provider: ^2.1.5
  permission_handler: ^11.2.0
  speech_to_text: ^7.3.0   # Updated version compatible with Flutter 3.8.1
  cupertino_icons: ^1.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/events.json
